version: "3.9"

networks:
  traefik-public:
    external: true
  wazuh-net:
    driver: overlay

volumes:
  wazuh-indexer-data:
    driver: local
  wazuh-manager-data:
    driver: local
  wazuh-dashboard-data:
    driver: local
  wazuh-certs-dashboard:
    driver: local
  wazuh-certs-indexer:
    driver: local

services:
  # ---------------- Wazuh Indexer ----------------
  wazuh-indexer:
    image: wazuh/wazuh-indexer:4.12.0
    networks:
      - wazuh-net
    volumes:
      - wazuh-indexer-data:/var/lib/wazuh-indexer
      - wazuh-certs:/etc/wazuh-indexer/certs:ro
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == node2
    healthcheck:
      test: ["CMD", "curl", "-f", "https://localhost:9200/_cluster/health --cacert /etc/wazuh/certs/ca.pem || exit 1"]
      interval: 10s
      retries: 10
      start_period: 30s

  # ---------------- Wazuh Manager ----------------
  wazuh-manager:
    image: wazuh/wazuh:4.12.0
    environment:
      - ELASTICSEARCH_HOST=wazuh-indexer
      - ELASTICSEARCH_PORT=9200
      - NODE_NAME=wazuh-manager
      - WAZUH_CERTS_PATH=/etc/wazuh/certs
    networks:
      - wazuh-net
    volumes:
      - wazuh-indexer-data:/var/lib/wazuh-indexer
      - wazuh-certs-indexer:/etc/wazuh-indexer/certs:ro
    depends_on:
      - wazuh-indexer
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == node2

  # ---------------- Wazuh Dashboard ----------------
  wazuh-dashboard:
    image: wazuh/wazuh-dashboard:4.12.0
    environment:
      - ELASTICSEARCH_HOST=wazuh-indexer
      - ELASTICSEARCH_PORT=9200
      - WAZUH_MANAGER_HOST=wazuh-manager
      - WAZUH_MANAGER_PORT=55000
      - WAZUH_CERTS_PATH=/etc/wazuh-dashboard/certs
    networks:
      - wazuh-net
      - traefik-public
    depends_on:
      - wazuh-indexer
      - wazuh-manager
    wazuh-dashboard:
    volumes:
      - wazuh-dashboard-data:/usr/share/wazuh-dashboard/data
      - wazuh-certs-dashboard:/etc/wazuh-dashboard/certs:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.wazuh-dashboard.rule=Host(`16.16.96.148`)"
      - "traefik.http.routers.wazuh-dashboard.entrypoints=websecure"
      - "traefik.http.routers.wazuh-dashboard.tls=true"
      - "traefik.http.routers.wazuh-dashboard.tls.certresolver=selfsigned"
      - "traefik.http.services.wazuh-dashboard.loadbalancer.server.port=5601"
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == node1
