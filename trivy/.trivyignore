# Here are known CVEs that we accept (e.g., false positives, vendor not fixing, non-exploitable in our environment)
# Ignore Wazuh internal private keys
/var/ossec/etc/ssl*.key
/var/ossec/etc/ssl*.cert
/etc/elasticsearch/certs/*.key
/etc/elasticsearch/certs/*.crt
